import { Router } from 'express';

const router = Router();

// Placeholder routes for Phase 4
router.get('/', (req, res) => {
  res.json({ message: 'Absence routes - to be implemented in Phase 4' });
});

export { router as absenceRoutes };
